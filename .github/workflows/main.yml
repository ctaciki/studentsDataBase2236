name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake build-essential libgtest-dev lcov

    - name: Build Google Test
      run: |
        cd /usr/src/googletest
        sudo cmake -S . -B build
        sudo cmake --build build
        sudo cp build/lib/lib*.a /usr/lib

    - name: Compile with coverage
      run: |
        g++ -fprofile-arcs -ftest-coverage -std=c++17 main.cpp \
            -I/usr/include/gtest -lgtest -lgtest_main -pthread -o program.out

    - name: Check coverage files
      run: |
        ls -l *.gcno || echo "No .gcno files found"

    - name: Execute tests
      run: ./program.out --run-tests

    - name: Check coverage data
      run: |
        ls -l *.gcda || echo "No .gcda files found"

    - name: Generate coverage
      run: |
        lcov --capture --directory . --output-file coverage.info --ignore-errors mismatch
        lcov --remove coverage.info '/usr/*' --output-file coverage.info
        lcov --list coverage.info
        PERCENT=$(lcov --list coverage.info | grep "lines" | awk '{print $2}' | sed 's/%//' | head -1 || echo "0")
        echo "COVERAGE=${PERCENT:-0}" >> $GITHUB_ENV

    - name: Create coverage badge
      uses: schneegans/dynamic-badges-action@v1.7.0
      with:
        auth: ${{ secrets.GIST_SECRET }}
        gistID: 56ceb89e202d8eb2b4d499bd8b387c46
        filename: coverage.json
        label: Coverage
        message: ${{ env.COVERAGE }}%
        color: green
        style: flat
